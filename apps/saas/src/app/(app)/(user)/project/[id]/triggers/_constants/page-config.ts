export const triggersPageConfig = {
    title: "Triggers",
    description:
        "Webhook events that invoke Devin. Workflows combine events from multiple triggers and run when your match condition is met (e.g. all selected triggers fire within a time window). Start by creating a trigger, then optionally add a workflow to orchestrate across triggers.",
};
